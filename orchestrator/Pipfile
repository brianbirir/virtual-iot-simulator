[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
grpcio = ">=1.62.0"
grpcio-tools = ">=1.62.0"
protobuf = ">=4.25.0"
typer = ">=0.12.0"
pydantic = ">=2.6.0"
pyyaml = ">=6.0.1"
rich = ">=13.7.0"
fastapi = ">=0.111.0"
uvicorn = {version = ">=0.29.0", extras = ["standard"]}

[dev-packages]
pytest = ">=8.1.0"
pytest-asyncio = ">=0.23.0"
httpx = ">=0.27.0"

[requires]
python_version = "3.12"
